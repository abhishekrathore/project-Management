angular.module('projectDev')
    .controller('loginSignupCtrl', loginSignupCtrl);
loginSignupCtrl.$inject = ['$scope', 'serverRequestService', 'Notification', '$q', '$location','$window'];
// Project Dev Controller
function loginSignupCtrl($scope, serverRequestService, Notification, $q, $location, $window) {
}
